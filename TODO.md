# TODO

- [x] Update `style.css` to add animated gradient + glow effect for hero buttons:
  - Apply to `.btn-group .btn` (covers both “Download CV” and “contact”).
  - Keep current `.btn` styles intact; add pseudo-element overlay and keyframes.
  - Add `prefers-reduced-motion: reduce` fallback.
- [ ] Verify in browser: hover both buttons.

